"""Feature modules. Each has router.py, schemas.py, service.py, repository.py, models.py.

Modules talk to each other through service interfaces, never another module's repository
or tables (architecture.md §2).
"""
